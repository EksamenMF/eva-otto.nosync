---

---

<div class="logos">
  <div class="logos-slide">
    <img src="../../public/img/flæsk.webp" alt="skilt" />
    <img src="../../public/img/" alt="ol" />
    <img src="../../public/img/" alt="tavle" />
    <img src="../../public/img/" alt="stemning2" />
    <img src="../../public/img/" alt="interiør" />
    <img src="../../public/img/" alt="fustache-haner" />
    <img src="../../public/img/" alt="øl" />
  </div>
  <div class="logos-slide">
    <img src="../../public/img/" alt="skilt" />
    <img src="../../public/img/" alt="ol" />
    <img src="../../public/img/" alt="tavle" />
    <img src="../../public/img/" alt="stemning2" />
    <img src="../../public/img/" alt="interiør" />
    <img src="../../public/img/" alt="fustache-haner" />
    <img src="../../public/img/" alt="øl" />
  </div>
</div>

<style>
  @media (min-width: 1200px) {
    @keyframes slide {
      from {
        transform: translateX(0);
      }
      to {
        transform: translateX(-100%);
      }
    }
    .logos {
      overflow: hidden;
      padding: 60px 0;
      white-space: nowrap;
      position: relative;
    }

    .logos:before,
    .logos:after {
      position: absolute;
      top: 0;
      width: 150px;
      height: 100%;
      content: "";
      z-index: 2;
    }

    .logos:before {
      left: 0;
      background: linear-gradient(to left, rgba(255, 255, 255, 0), #235347);
    }

    .logos:after {
      right: 0;
      background: linear-gradient(to right, rgba(255, 255, 255, 0), #235347);
    }
    .logos-slide {
      display: inline-block;
      animation: 35s slide infinite linear;
    }

    .logos-slide img {
      height: 400px; /* Adjust height as needed */
      margin: 0 40px;
    }
  }
  @media (min-width: 769px) and (max-width: 1200px) {
    @keyframes slide {
      from {
        transform: translateX(0);
      }
      to {
        transform: translateX(-100%);
      }
    }

    .logos {
      overflow: hidden;
      padding: 60px 0;
      white-space: nowrap;
      position: relative;
    }

    .logos:before,
    .logos:after {
      position: absolute;
      top: 0;
      width: 150px;
      height: 100%;
      content: "";
      z-index: 2;
    }

    .logos:before {
      left: 0;
      background: linear-gradient(to left, rgba(255, 255, 255, 0), #235347);
    }

    .logos:after {
      right: 0;
      background: linear-gradient(to right, rgba(255, 255, 255, 0), #235347);
    }

    /* .logos:hover .logos-slide {
          animation-play-state: paused;
        } */

    .logos-slide {
      display: inline-block;
      animation: 35s slide infinite linear;
    }

    .logos-slide img {
      height: 400px; /* Adjust height as needed */
      margin: 0 40px;
    }
  }
  @media (min-width: 481px) and (max-width: 768px) {
    @keyframes slide {
      from {
        transform: translateX(0);
      }
      to {
        transform: translateX(-100%);
      }
    }

    .logos {
      overflow: hidden;
      padding: 60px 0;
      white-space: nowrap;
      position: relative;
    }

    .logos:before,
    .logos:after {
      position: absolute;
      top: 0;
      width: 50px;
      height: 100%;
      content: "";
      z-index: 2;
    }

    .logos:before {
      left: 0;
      background: linear-gradient(to left, rgba(255, 255, 255, 0), #235347);
    }

    .logos:after {
      right: 0;
      background: linear-gradient(to right, rgba(255, 255, 255, 0), #235347);
    }

    .logos-slide {
      display: inline-block;
      animation: 35s slide infinite linear;
    }

    .logos-slide img {
      height: 400px; /* Adjust height as needed */
      margin: 0 40px;
    }
  }

  @media (max-width: 480px) {
    @keyframes slide {
      from {
        transform: translateX(0);
      }
      to {
        transform: translateX(-100%);
      }
    }

    .logos {
      overflow: hidden;
      padding: 60px 0;
      white-space: nowrap;
      position: relative;
    }

    .logos:before,
    .logos:after {
      position: absolute;
      top: 0;
      width: 50px;
      height: 100%;
      content: "";
      z-index: 2;
    }

    .logos:before {
      left: 0;
      background: linear-gradient(to left, rgba(255, 255, 255, 0), #235347);
    }

    .logos:after {
      right: 0;
      background: linear-gradient(to right, rgba(255, 255, 255, 0), #235347);
    }

    .logos-slide {
      display: inline-block;
      animation: 35s slide infinite linear;
    }

    .logos-slide img {
      height: 400px; /* Adjust height as needed */
      margin: 0 40px;
    }
  }
</style>
